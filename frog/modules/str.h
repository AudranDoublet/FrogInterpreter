#ifndef __FROGMODULE_STR_H
#define __FROGMODULE_STR_H
FrogObject *str_toLowerCase(FrogObject **args, size_t count, stack *p);

FrogObject *str_toUpperCase(FrogObject **args, size_t count, stack *p);
#endif
